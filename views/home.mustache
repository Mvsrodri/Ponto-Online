<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Registro de Ponto</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body class="background">

  {{>HeaderLogin}}
  <div class="Ponto-container">
    <div class="button- container">
      <form action="/carga-registros" method="POST">
        <button type="submit">Realizar Carga de Registros</button>
      </form>
      <form action="/registrar" method="GET">
        <button type="submit">Registrar novo Ponto</button>
      </form>
    </div>
    <h1 class="title">Pontos Registrados</h1>
    <div class="table-container">
      <select id="ordenarPorData">
        <option value="sel">Selecionar</option>
        <option value="asc">Ordenar por data (crescente)</option>
        <option value="desc">Ordenar por data (decrescente)</option>
      </select>
      <table>
        <thead>
          <tr>
            <th>Data</th>
            <th>Entrada 1</th>
            <th>Saída 1</th>
            <th>Entrada 2</th>
            <th>Saída 2</th>
          </tr>
        </thead>
        <tbody>
          {{#pontos}}
            <tr>
              <td>{{dataFormatada}}</td>    
              <td>{{entrada1}}</td>
              <td>{{saida1}}</td>
              <td>{{entrada2}}</td>
              <td>{{saida2}}</td>
              <td>
                <a href="/editar/{{_id}}">
                  <i class="fas fa-edit"></i>
                </a>
                <a href="/excluir/{{_id}}">
                  <i class="fas fa-trash-alt"></i>
                </a>
              </td>
            </tr>
          {{/pontos}}
        </tbody>
      </table>
    </div>
  </div>

  {{>Footer}}
  <script src="/javascripts/filtro.js"></script>
</body>
</html>
